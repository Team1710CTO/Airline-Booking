
public class UserVerification {
	public static boolean verifyUser(String user, String password) {
		boolean success; // set success equal to wether or not the user-password combination is valid
		/*
		 * use a for loop to check the to inputted strings with the array in Users.java, if the combination
		 * exists, set success equal to true
		 * ask hal or google "Java multidimensional array search" if lost ;)
		*/
		return success;
	}
}
